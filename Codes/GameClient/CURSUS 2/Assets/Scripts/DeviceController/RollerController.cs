using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RollerController : MonoBehaviour {
	/*
	 * 
	 * 안씀. SerialPortController에 다 처리함.
    private bool during_setting = false;

    private int level;

    private void Awake()
    {
        level = 0;
    }

    // Use this for initialization
    void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}

    public void SetLevel(int l)
    {
        level = l;
        Debug.Log("Start set Roller Level to " + level);
        Debug.Log("Setted Roller Level to " + level);

        //StartCoroutine(SetRollerLevel());
    }

    IEnumerator SetRollerLevel()
    {
        // Waiting
        Debug.Log("Start set Roller Level to " + level);
        yield return new WaitForSeconds(3.0f);
        Debug.Log("Setted Roller Level to " + level);
        // Set Device
    }*/

}
